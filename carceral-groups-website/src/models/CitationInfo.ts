export type CitationInfo = {
    author?: string,
    title: string,
    placeOfPublication: string,
    publisher: string,
    yearOfPublication: string,
    pageNumbers?: string,
    url?: string,
    accessedDate?: string
}